# Ignore everything by default
*

# But not this file
!.gitignore

# ─────────────────────────────────────────────
# Shell
# ─────────────────────────────────────────────
!.zshrc
!.zshenv
!.profile
!.gitconfig

# ─────────────────────────────────────────────
# Tmux
# ─────────────────────────────────────────────
!.tmux.conf
!.tmux/themes/
!.tmux/themes/**

# ─────────────────────────────────────────────
# Gemini CLI (only config, NO auth/secrets)
# ─────────────────────────────────────────────
!.gemini/
!.gemini/settings.json
!.gemini/GEMINI.md
# oauth_creds.json, google_accounts.json, installation_id stay ignored

# ─────────────────────────────────────────────
# Claude Code
# ─────────────────────────────────────────────
!CLAUDE.md
!.claude/
!.claude/settings.json
!.claude/commands/
!.claude/commands/**
!.claude/skills/
!.claude/skills/**
!.claude/plugins/
.claude/plugins/cache/*
# Exclude compiled skill artifacts
.claude/skills/dist/
!.claude/hooks/
!.claude/hooks/**
# Exclude hook logs
.claude/hooks/*.log
!.claude/scripts/
!.claude/scripts/**
!.claude/agents/
!.claude/agents/**

# ─────────────────────────────────────────────
# Codex CLI (config only, NO auth/secrets)
# ─────────────────────────────────────────────
!.codex/
!.codex/AGENTS.md
!.codex/config.toml
!.codex/rules/
!.codex/rules/**
# Exclude system-installed stuff (cloned repos/boilerplate)
.codex/skills/
.codex/superpowers/
# Exclude sensitive/runtime data
.codex/auth.json
.codex/history.jsonl
.codex/sessions/
.codex/log/
.codex/version.json

# ─────────────────────────────────────────────
# .config/ tools
# ─────────────────────────────────────────────
!.config/

# Neovim
!.config/nvim/
!.config/nvim/**
# Exclude lazy.nvim lock file (auto-generated)
.config/nvim/lazy-lock.json

# Exclude macOS junk
.config/.DS_Store

# Terminal
!.config/ghostty/
!.config/ghostty/**

# File managers & tools
!.config/yazi/
!.config/yazi/**
!.config/lsd/
!.config/lsd/**
!.config/bat/
!.config/bat/**

# Shells
!.config/fish/
!.config/fish/**

# Editors
!.config/zed/
!.config/zed/**

# Multiplexers
!.config/zellij/
!.config/zellij/**

# GitHub CLI (exclude hosts.yml - has OAuth token)
!.config/gh/
!.config/gh/config.yml

# Git (global ignore patterns)
!.config/git/
!.config/git/ignore

